services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./requests.db:/app/requests.db
      - ./requests.log:/app/requests.log
    environment:
      - GO_ENV=production
